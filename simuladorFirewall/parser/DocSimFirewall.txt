The Language SimFirewall
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of SimFirewall==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in SimFirewall are the following:
  | ``ACCEPT`` | ``CLOSE`` | ``CLOSE_WAIT`` | ``DROP``
  | ``END_CONNECTIONS`` | ``END_HOST`` | ``END_PACKAGES`` | ``END_RULES``
  | ``ESTABLISHED`` | ``FIN_WAIT`` | ``FORDWARD`` | ``ICMP``
  | ``INIT_CONNECTIONS`` | ``INIT_HOST`` | ``INIT_PACKAGES`` | ``INIT_RULES``
  | ``INPUT`` | ``INVALID`` | ``LAST_ACK`` | ``LISTEN``
  | ``NEW`` | ``NONE`` | ``OUTPUT`` | ``POSTROUTING``
  | ``PREROUTING`` | ``REJECT`` | ``RELATED`` | ``RETURN``
  | ``SYN_RECV`` | ``SYN_SENT`` | ``TCP`` | ``TIME_WAIT``
  | ``UDP`` | ``Use`` | ``all`` | ``dcode``
  | ``did`` | ``dport`` | ``dst`` | ``dtype``
  | ``filter`` | ``icmp`` | ``interfaz`` | ``ip``
  | ``mac`` | ``mangle`` | ``nat`` | ``puerto``
  | ``scode`` | ``sid`` | ``spot`` | ``src``
  | ``state`` | ``stype`` | ``tcp`` | ``ttl``
  | ``udp`` |  |  |

The symbols used in SimFirewall are the following:
  | ; | src-mac | -t | -A
  | -D | -R | -I | -F
  | -P | -p | ! | -s
  | -d | -i | -o | -m
  | --state | --mac-sourse | , | .
  | / | --dport | --sport | :
  | --tcp-flags | --icmp-type | -j |

===Comments===
Single-line comments begin with #.Multiple-line comments are  enclosed with #/ and /#.

==The syntactic structure of SimFirewall==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Function]//
  | //Function// | -> | ``INIT_RULES`` //[Rule]// ``END_RULES``
  |  |  **|**  | ``INIT_HOST`` //[Inter]// ``END_HOST``
  |  |  **|**  | ``INIT_PACKAGES`` //[Package]// ``END_PACKAGES``
  |  |  **|**  | ``INIT_CONNECTIONS`` //[Connection]// ``END_CONNECTIONS``
  | //Connection// | -> | ``ip`` //Ip// ``puerto`` //Integer// ``ip`` //Ip// ``puerto`` //Integer// ``state`` //State// ``;``
  | //Rule// | -> | //Tab// //Com// //Mat// //Target// ``;``
  | //Inter// | -> | ``interfaz`` //String// ``ip`` //Ip// ``mac`` //String// ``;``
  | //Package// | -> | ``TCP`` ``state`` //IntEstTCP// ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``spot`` //Integer// ``dport`` //Integer// ``ttl`` //Integer// ``Use`` //Integer// ``interfaz`` //String// ``;``
  |  |  **|**  | ``UDP`` ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``spot`` //Integer// ``dport`` //Integer// ``Use`` //Integer// ``interfaz`` //String// ``;``
  |  |  **|**  | ``ICMP`` ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``stype`` //Integer// ``scode`` //Integer// ``sid`` //Integer// ``dtype`` //Integer// ``dcode`` //Integer// ``did`` //Integer// ``Use`` //Integer// ``interfaz`` //String// ``;``
  | //[Function]// | -> | **eps**
  |  |  **|**  | //Function// //[Function]//
  | //[Connection]// | -> | **eps**
  |  |  **|**  | //Connection// //[Connection]//
  | //[Rule]// | -> | **eps**
  |  |  **|**  | //Rule// //[Rule]//
  | //[Inter]// | -> | **eps**
  |  |  **|**  | //Inter// //[Inter]//
  | //[Package]// | -> | **eps**
  |  |  **|**  | //Package// //[Package]//
  | //[Tab]// | -> | **eps**
  |  |  **|**  | //Tab// //[Tab]//
  | //[Com]// | -> | **eps**
  |  |  **|**  | //Com// //[Com]//
  | //[Mat]// | -> | **eps**
  |  |  **|**  | //Mat// //[Mat]//
  | //Tab// | -> | ``-t`` ``nat``
  |  |  **|**  | ``-t`` ``mangle``
  |  |  **|**  | ``-t`` ``filter``
  |  |  **|**  | **eps**
  | //Com// | -> | ``-A`` //Cha//
  |  |  **|**  | ``-D`` //Cha//
  |  |  **|**  | ``-R`` //Cha// //Integer//
  |  |  **|**  | ``-I`` //Cha// //Integer//
  |  |  **|**  | ``-F`` //Cha//
  |  |  **|**  | ``-P`` //Target//
  |  |  **|**  | **eps**
  | //Cha// | -> | ``INPUT``
  |  |  **|**  | ``OUTPUT``
  |  |  **|**  | ``FORDWARD``
  |  |  **|**  | ``PREROUTING``
  |  |  **|**  | ``POSTROUTING``
  | //Mat// | -> | ``-p`` //[Protocolo]// //[Pop]//
  |  |  **|**  | ``-p`` ``!`` //[Protocolo]// //[Pop]//
  |  |  **|**  | ``-s`` //Ip//
  |  |  **|**  | ``-d`` //Ip//
  |  |  **|**  | ``-i`` //String//
  |  |  **|**  | ``-o`` //String//
  |  |  **|**  | ``-m`` ``--state`` //State//
  |  |  **|**  | ``-m`` ``mac`` ``--mac-sourse`` //String//
  |  |  **|**  | ``-m`` ``mac`` ``--mac-sourse`` ``!`` //String//
  |  |  **|**  | **eps**
  | //[Protocolo]// | -> | **eps**
  |  |  **|**  | //Protocolo//
  |  |  **|**  | //Protocolo// ``,`` //[Protocolo]//
  | //Protocolo// | -> | ``udp``
  |  |  **|**  | ``tcp``
  |  |  **|**  | ``icmp``
  |  |  **|**  | ``all``
  | //Ip// | -> | //Integer// ``.`` //Integer// ``.`` //Integer// ``.`` //Integer// ``/`` //Integer//
  |  |  **|**  | //Integer// ``.`` //Integer// ``.`` //Integer// ``.`` //Integer//
  | //[Pop]// | -> | **eps**
  |  |  **|**  | //Pop//
  |  |  **|**  | //Pop// ``,`` //[Pop]//
  | //[State]// | -> | **eps**
  |  |  **|**  | //State//
  |  |  **|**  | //State// ``,`` //[State]//
  | //[Target]// | -> | **eps**
  |  |  **|**  | //Target//
  |  |  **|**  | //Target// ``,`` //[Target]//
  | //Pop// | -> | ``--dport`` //Integer//
  |  |  **|**  | ``--dport`` ``!`` //Integer//
  |  |  **|**  | ``--sport`` //Integer//
  |  |  **|**  | ``--sport`` ``!`` //Integer//
  |  |  **|**  | ``--dport`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--dport`` ``!`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--sport`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--sport`` ``!`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--tcp-flags`` //IntEstTCP//
  |  |  **|**  | ``--tcp-flags`` ``!`` //IntEstTCP//
  |  |  **|**  | ``--tcp-flags`` ``!`` //IntEstTCP//
  |  |  **|**  | ``--icmp-type`` //Integer//
  |  |  **|**  | ``--icmp-type`` ``!`` //Integer//
  |  |  **|**  | **eps**
  | //State// | -> | ``NEW``
  |  |  **|**  | ``INVALID``
  |  |  **|**  | ``ESTABLISHED``
  |  |  **|**  | ``RELATED``
  | //Target// | -> | ``-j`` ``ACCEPT``
  |  |  **|**  | ``-j`` ``DROP``
  |  |  **|**  | ``-j`` ``REJECT``
  |  |  **|**  | ``-j`` ``RETURN``
  |  |  **|**  | **eps**
  | //[IntEstTCP]// | -> | **eps**
  |  |  **|**  | //IntEstTCP// //[IntEstTCP]//
  | //IntEstTCP// | -> | ``NONE``
  |  |  **|**  | ``ESTABLISHED``
  |  |  **|**  | ``SYN_SENT``
  |  |  **|**  | ``SYN_RECV``
  |  |  **|**  | ``FIN_WAIT``
  |  |  **|**  | ``TIME_WAIT``
  |  |  **|**  | ``CLOSE``
  |  |  **|**  | ``CLOSE_WAIT``
  |  |  **|**  | ``LAST_ACK``
  |  |  **|**  | ``LISTEN``


