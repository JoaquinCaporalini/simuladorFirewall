The Language SimFirewall
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of SimFirewall==

===Literals===
String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Integer literals //Integer// are nonempty sequences of digits.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in SimFirewall are the following:
  | ``ACCEPT`` | ``ACK`` | ``CLOSE`` | ``CLOSE_WAIT``
  | ``DROP`` | ``END_HOST`` | ``END_PACKAGES`` | ``END_RULES``
  | ``ESTABLISHED`` | ``FIN`` | ``FIN_WAIT`` | ``FORDWARD``
  | ``ICMP`` | ``INIT_HOST`` | ``INIT_PACKAGES`` | ``INIT_RULES``
  | ``INPUT`` | ``INVALID`` | ``LAST_ACK`` | ``LISTEN``
  | ``NEW`` | ``NONE`` | ``OUTPUT`` | ``REGECT``
  | ``RELATED`` | ``RETURN`` | ``SYN`` | ``SYN_RECV``
  | ``SYN_SENT`` | ``TCP`` | ``TIME_WAIT`` | ``UDP``
  | ``Use`` | ``all`` | ``dcode`` | ``did``
  | ``dport`` | ``dst`` | ``dtype`` | ``filter``
  | ``icmp`` | ``interfaz`` | ``ip`` | ``mac``
  | ``mangle`` | ``nat`` | ``scode`` | ``sid``
  | ``spot`` | ``src`` | ``state`` | ``stype``
  | ``tcp`` | ``ttl`` | ``udp`` |

The symbols used in SimFirewall are the following:
  | ; | src-mac | -t | -A
  | -D | -R | -I | -F
  | -P | -p | ! | -s
  | -d | -i | -o | -f
  | -m | --state | --mac-sourse | ,
  | . | / | --dport | --sport
  | : | --tcp-flags | --icmp-type | -j

===Comments===
Single-line comments begin with #.Multiple-line comments are  enclosed with #/ and /#.

==The syntactic structure of SimFirewall==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Function]//
  | //Function// | -> | ``INIT_RULES`` //[Rule]// ``END_RULES``
  |  |  **|**  | ``INIT_HOST`` //[Inter]// ``END_HOST``
  |  |  **|**  | ``INIT_PACKAGES`` //[PInfo]// ``END_PACKAGES``
  | //Rule// | -> | //Tab// //Com// //Mat// //Target// ``;``
  | //Inter// | -> | ``interfaz`` //String// ``ip`` //Ip// ``mac`` //String// ``;``
  | //PInfo// | -> | ``TCP`` ``state`` //IntEstTCP// ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``spot`` //Integer// ``dport`` //Integer// ``ttl`` //Integer// ``Use`` //Integer// ``;``
  |  |  **|**  | ``UDP`` ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``spot`` //Integer// ``dport`` //Integer// ``Use`` //Integer// ``;``
  |  |  **|**  | ``ICMP`` ``src`` //Ip// ``src-mac`` //String// ``dst`` //Ip// ``stype`` //Integer// ``scode`` //Integer// ``sid`` //Integer// ``dtype`` //Integer// ``dcode`` //Integer// ``did`` //Integer// ``Use`` //Integer// ``;``
  | //[Function]// | -> | **eps**
  |  |  **|**  | //Function// //[Function]//
  | //[Rule]// | -> | **eps**
  |  |  **|**  | //Rule// //[Rule]//
  | //[Inter]// | -> | **eps**
  |  |  **|**  | //Inter// //[Inter]//
  | //[PInfo]// | -> | **eps**
  |  |  **|**  | //PInfo// //[PInfo]//
  | //[Tab]// | -> | **eps**
  |  |  **|**  | //Tab// //[Tab]//
  | //[Com]// | -> | **eps**
  |  |  **|**  | //Com// //[Com]//
  | //[Mat]// | -> | **eps**
  |  |  **|**  | //Mat// //[Mat]//
  | //Tab// | -> | ``-t`` ``nat``
  |  |  **|**  | ``-t`` ``mangle``
  |  |  **|**  | ``-t`` ``filter``
  |  |  **|**  | **eps**
  | //Com// | -> | ``-A`` //Cha//
  |  |  **|**  | ``-D`` //Cha//
  |  |  **|**  | ``-R`` //Cha// //Integer//
  |  |  **|**  | ``-I`` //Cha// //Integer//
  |  |  **|**  | ``-F`` //Cha//
  |  |  **|**  | ``-P`` //Cha// //Target//
  |  |  **|**  | **eps**
  | //Cha// | -> | ``INPUT``
  |  |  **|**  | ``OUTPUT``
  |  |  **|**  | ``FORDWARD``
  | //Mat// | -> | ``-p`` //[Protocolo]// //[Pop]//
  |  |  **|**  | ``-p`` ``!`` //[Protocolo]// //[Pop]//
  |  |  **|**  | ``-s`` //Ip//
  |  |  **|**  | ``-d`` //Ip//
  |  |  **|**  | ``-i`` //String//
  |  |  **|**  | ``-o`` //String//
  |  |  **|**  | ``-f``
  |  |  **|**  | ``-f`` ``!``
  |  |  **|**  | ``-m`` ``--state`` //State//
  |  |  **|**  | ``-m`` ``mac`` ``--mac-sourse``
  |  |  **|**  | ``-m`` ``mac`` ``--mac-sourse`` ``!``
  |  |  **|**  | **eps**
  | //[Protocolo]// | -> | **eps**
  |  |  **|**  | //Protocolo//
  |  |  **|**  | //Protocolo// ``,`` //[Protocolo]//
  | //Protocolo// | -> | ``udp``
  |  |  **|**  | ``tcp``
  |  |  **|**  | ``icmp``
  |  |  **|**  | ``all``
  | //Ip// | -> | //Integer// ``.`` //Integer// ``.`` //Integer// ``.`` //Integer// ``/`` //Integer//
  |  |  **|**  | //Integer// ``.`` //Integer// ``.`` //Integer// ``.`` //Integer//
  | //[Pop]// | -> | **eps**
  |  |  **|**  | //Pop//
  |  |  **|**  | //Pop// ``,`` //[Pop]//
  | //[Flags]// | -> | **eps**
  |  |  **|**  | //Flags//
  |  |  **|**  | //Flags// ``,`` //[Flags]//
  | //[State]// | -> | **eps**
  |  |  **|**  | //State//
  |  |  **|**  | //State// ``,`` //[State]//
  | //[Target]// | -> | **eps**
  |  |  **|**  | //Target//
  |  |  **|**  | //Target// ``,`` //[Target]//
  | //Pop// | -> | ``--dport`` //Integer//
  |  |  **|**  | ``--dport`` ``!`` //Integer//
  |  |  **|**  | ``--sport`` //Integer//
  |  |  **|**  | ``--sport`` ``!`` //Integer//
  |  |  **|**  | ``--dport`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--dport`` ``!`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--sport`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--sport`` ``!`` //Integer// ``:`` //Integer//
  |  |  **|**  | ``--tcp-flags`` //Flags//
  |  |  **|**  | ``--tcp-flags`` ``!`` //Flags//
  |  |  **|**  | ``--tcp-flags`` ``!`` //Flags//
  |  |  **|**  | ``--icmp-type`` //Flags//
  |  |  **|**  | ``--icmp-type`` ``!`` //Flags//
  |  |  **|**  | **eps**
  | //Flags// | -> | ``SYN``
  |  |  **|**  | ``FIN``
  |  |  **|**  | ``ACK``
  | //State// | -> | ``NEW``
  |  |  **|**  | ``INVALID``
  |  |  **|**  | ``ESTABLISHED``
  |  |  **|**  | ``RELATED``
  | //Target// | -> | ``-j`` ``ACCEPT``
  |  |  **|**  | ``-j`` ``DROP``
  |  |  **|**  | ``-j`` ``REGECT``
  |  |  **|**  | ``-j`` ``RETURN``
  |  |  **|**  | **eps**
  | //[IntEstTCP]// | -> | **eps**
  |  |  **|**  | //IntEstTCP// //[IntEstTCP]//
  | //IntEstTCP// | -> | ``NONE``
  |  |  **|**  | ``ESTABLISHED``
  |  |  **|**  | ``SYN_SENT``
  |  |  **|**  | ``SYN_RECV``
  |  |  **|**  | ``FIN_WAIT``
  |  |  **|**  | ``TIME_WAIT``
  |  |  **|**  | ``CLOSE``
  |  |  **|**  | ``CLOSE_WAIT``
  |  |  **|**  | ``LAST_ACK``
  |  |  **|**  | ``LISTEN``


